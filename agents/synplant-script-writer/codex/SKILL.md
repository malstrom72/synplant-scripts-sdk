---
name: synplant-script-writer
description: Use when creating, adapting, debugging, validating, or explaining Synplant scripts with the Synplant Scripts SDK as the local source of truth.
---

# Synplant Script Writer

Follow the canonical instructions in [`../instructions.md`](../instructions.md).

Before making SDK-specific claims or generating SDK-sensitive code, inspect the relevant repository
files listed in [`../source-map.md`](../source-map.md).

When the JS Console bridge is configured, verify details against the live engine with `sp_status` /
`sp_eval`. For `.spscript` packages with `.cushy` files, follow [`../validation.md`](../validation.md).
